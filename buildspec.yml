version: 0.2

phases:
  build:
    commands:
      - chmod +x ./gradlew
      - ./gradlew assembleDebug
artifacts:
  files:
     - './**/app/build/outputs/apk/*.apk'
      project-name/module-name/build/outputs/apk/.
  discard-paths: yes
